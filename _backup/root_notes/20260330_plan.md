# 2026-03-30 실행 계획서 — 파이프라인 실험 + 후보군 탐색

> **목표**: Silo B 파이프라인 실험 돌려서 SSTR2 선택적 펩타이드 후보군 확보
> **환경**: H100 NVL GPU 2번, Ollama qwen3:8b, Boltz2 로컬 (single-seq), ESMFold 배치
> **전제**: Phase 0~1.5 완료, Boltz2 해결, CI 통과

---

## Step 0: 실험 전 사전 검증 (30분)

### 0.1 DiffPepBuilder 실제 동작 테스트
```bash
source ~/miniforge3/etc/profile.d/conda.sh && conda activate bio-tools
python -c "
from pipeline_local.core.local_runner import LocalModelRunner
pdb = open('AgenticAI4SCIENCE_pyrosetta_track/repos/ai4sci-kaeri/data/fold_test1_model_0.pdb').read()
runner = LocalModelRunner()
result = runner.run('diffpepbuilder', {
    'receptor_pdb': pdb,
    'peptide_sequence': 'AGCKNFFWKTFTSC',
})
print('Keys:', list(result.keys()))
"
```
- 성공 시: 다음 단계
- 실패 시: Boltz가 폴백이므로 진행 가능. 에러 기록만 해두고 넘어감

### 0.2 Boltz2 파이프라인 내 호출 테스트
```bash
python -c "
from pipeline_local.core.local_runner import LocalModelRunner
runner = LocalModelRunner()
result = runner.run('boltz', {
    'receptor_pdb': open('...data/fold_test1_model_0.pdb').read(),
    'peptide_sequence': 'AGCKNFFWKTFTSC',
})
print('Keys:', list(result.keys()))
print('Confidence:', result.get('confidence', {}))
"
```
- Boltz wrapper가 `--no_kernels`, `msa: "empty"`, `--num_workers 0` 로 호출되는지 확인
- confidence JSON에 ipTM, pTM 포함되는지

### 0.3 ESMFold 배치 모드 재확인
```bash
python -c "
from pipeline_local.core.local_runner import LocalModelRunner
runner = LocalModelRunner()
result = runner.run('esmfold', {
    'sequences': [
        {'seq_id': 't1', 'sequence': 'AGCKNFFWKTFTSC'},
        {'seq_id': 't2', 'sequence': 'AGCKSFFWKTFTSC'},
        {'seq_id': 't3', 'sequence': 'AGCKDFFWKTFTSC'},
    ]
})
print(f'Results: {len(result[\"results\"])}')
for r in result['results']:
    print(f'  {r[\"seq_id\"]}: pLDDT={r[\"mean_plddt\"]:.1f}')
"
```

### 0.4 RFdiffusion 파이프라인 호출 확인 (Silo A용)
```bash
python -c "
from pipeline_local.core.local_runner import LocalModelRunner
pdb = open('...data/fold_test1_model_0.pdb').read()
runner = LocalModelRunner()
result = runner.run('rfdiffusion', {
    'input_pdb': pdb,
    'contigs': 'B1-100/0 10-20',
    'random_seed': 42,
})
print('SUCCESS:', list(result.keys()))
"
```

### 0.5 Backend + Frontend 기동
```bash
source ~/.zshrc
conda activate bio-tools
nohup python -m pipeline_local.backend.main > /tmp/backend.log 2>&1 &
cd AgenticAI4SCIENCE_pyrosetta_track/repos/ai4sci-kaeri/frontend
nohup npm run dev > /tmp/frontend.log 2>&1 &
sleep 3
curl -s http://localhost:8787/api/health
curl -s http://localhost:5173 -o /dev/null -w "%{http_code}"
```

---

## Step 1: Silo B 3 Iteration 실험 (예상 1~2시간)

### 1.1 실행
```bash
source ~/miniforge3/etc/profile.d/conda.sh && conda activate bio-tools
cd /home/dongjukim/Documents/workspace/repos/SST14-M_scr

# GPU 모니터링 (백그라운드)
nvidia-smi --query-gpu=timestamp,index,memory.used,utilization.gpu \
    --format=csv -l 5 > runs_local/gpu_monitor_silob.csv 2>&1 &
GPU_PID=$!

# Silo B 실행
python -m pipeline_local.run_pipeline_local \
    --approach-b \
    --iterations 3 \
    --output-dir runs_local/silob_experiment_01 \
    --llm-model qwen3:8b \
    2>&1 | tee runs_local/silob_experiment_01.log

kill $GPU_PID
```

### 1.2 예상 소요 시간
| Step | 내용 | 예상 시간 |
|------|------|----------|
| Step01 | Receptor 준비 (기존 CIF) | <1초 |
| Step03b | BLOSUM62 mutation (128개) | <1초 |
| Step04 | ESMFold 배치 (128개) | ~70초 |
| Step05 | Boltz2 도킹 (통과 후보만) | ~13초 × N개 |
| Step06 | PyRosetta FlexPepDock | ~10분 × M개 |
| Step07 | 분석 | <1분 |
| LLM | Planner/Critic/Reporter | ~30초 (fallback 시 0초) |
| **총 1 iter** | | **~15~30분** |
| **3 iter** | | **~1~2시간** |

### 1.3 모니터링 포인트
- status.json 실시간 확인: `watch -n2 'cat /tmp/pipeline_local_status.json | python3 -m json.tool'`
- GPU 사용량: `nvidia-smi -l 5`
- 각 iteration에서:
  - QC 통과율 (29/128 이상 목표)
  - Boltz ipTM 분포
  - Rosetta ddG 분포
  - Critic이 어떤 피드백을 주는지

### 1.4 실험 완료 후 확인
```bash
# 결과 요약
cat runs_local/silob_experiment_01/*/08_reports/final_report.md
# 후보 테이블
cat runs_local/silob_experiment_01/*/rank_table.csv
# Step별 시간
grep "\[Step\].*완료" runs_local/silob_experiment_01.log
```

---

## Step 2: Silo A 3 Iteration 실험 (예상 3~5시간)

### 2.1 실행
```bash
python -m pipeline_local.run_pipeline_local \
    --no-approach-b \
    --iterations 3 \
    --output-dir runs_local/siloa_experiment_01 \
    --llm-model qwen3:8b \
    2>&1 | tee runs_local/siloa_experiment_01.log
```

### 2.2 예상 소요 시간
| Step | 내용 | 예상 시간 |
|------|------|----------|
| Step01 | Receptor 준비 | <1초 |
| Step02 | RFdiffusion 백본 생성 (10개, 순차) | ~5분 × 10 = ~50분 |
| Step03 | ProteinMPNN 서열 설계 | ~1분 × 10 |
| Step04 | ESMFold 배치 (80개) | ~50초 |
| Step05 | Boltz2 도킹 | ~13초 × N개 |
| Step06 | PyRosetta | ~10분 × M개 |
| **총 1 iter** | | **~1.5~2시간** |
| **3 iter** | | **~4.5~6시간** |

### 2.3 주의사항
- RFdiffusion이 GPU 2번에서 실행되므로 Silo B와 동시 실행 불가
- Step02에서 max_workers=1 (순차) — GPU 메모리 충돌 방지
- ProteinMPNN wrapper가 아직 실제 테스트 안 됨 — Step 0.1에서 실패 시 Silo A 보류

---

## Step 3: 결과 분석 + 후보군 선별 (1시간)

### 3.1 통합 랭킹
```bash
python -c "
# Silo A + Silo B 결과 통합
import json, csv
# ... rank_table.csv 로드 → 통합 → 상위 K개 추출
"
```

### 3.2 선별 기준
| 기준 | 값 | 의미 |
|------|-----|------|
| ddG | ≤ -5.0 kcal/mol | Rosetta 결합 에너지 |
| pLDDT | ≥ 60 | 구조 신뢰도 |
| clash | ≤ 10 | 원자 충돌 |
| Boltz ipTM | ≥ 0.7 | 인터페이스 신뢰도 |

### 3.3 후보군 출력
- 상위 10개 후보 서열 + ddG + pLDDT
- PDB 파일 저장
- Mol* 3D 뷰어에서 시각화

---

## Step 4: Off-target Selectivity 분석 (1~2시간)

### 4.1 상위 후보에 대해 SSTR1/3/4/5 도킹
```bash
# API로 실행
curl -X POST http://localhost:8787/api/selectivity/run \
    -H "Content-Type: application/json" \
    -d '{
        "candidate_ids": ["top1", "top2", ...],
        "candidate_sequences": [
            {"seq_id": "top1", "sequence": "AGCKNFFWKTFTSC", "sstr2_ddg": -8.5},
            ...
        ]
    }'
```

### 4.2 또는 CLI로 직접
```python
from pipeline_local.core.selectivity_runner import SelectivityRunner
runner = SelectivityRunner()
results = runner.run_selectivity_analysis(
    candidates=[...],
    receptor_paths={
        "sstr1": "data/somatostatin_receptor/SSTR1_9IK8.cif",
        "sstr3": "data/somatostatin_receptor/SSTR3_8XIR.cif",
        "sstr4": "data/somatostatin_receptor/SSTR4_7XMT.cif",
        "sstr5": "data/somatostatin_receptor/SSTR5_8ZBJ.cif",
    },
    sstr2_ddgs={"top1": -8.5, ...},
)
```

### 4.3 결과 해석
| Tier | WSM 기준 | 의미 | 배수 |
|------|---------|------|------|
| Tier 3 | ≤ -3.0 | 우수 (임상 개발 유망) | ~134× |
| Tier 2 | ≤ -2.0 | 권장 (lanreotide 수준) | ~26× |
| Tier 1 | ≤ -1.5 | 최소 (octreotide 수준) | ~11× |
| Tier 0 | > -1.5 | 불합격 | - |

---

## Step 5: Critic 확장 (병렬 진행 가능, 2시간)

실험 돌리는 동안 병렬로 진행.

### 5.1 FAILURE_ACTION_MAP 확장 (6 → 19)
수정 대상: `AG_src/agents/critic.py` — **원본 불변 원칙**에 따라 pipeline_local/에 래퍼 작성

```python
# pipeline_local/agents/critic_extended.py
from AG_src.agents.critic import ScientistCriticAgent, FAILURE_ACTION_MAP

EXTENDED_FAILURE_MAP = {
    **FAILURE_ACTION_MAP,
    "WEAK_INTERFACE": { ... },
    "BROKEN_DISULFIDE": { ... },
    "UNSTABLE_STRUCTURE": { ... },
    "GEOMETRIC_VIOLATION": { ... },
    "FOLD_DEVIATION": { ... },
    "LOW_DOCK_CONFIDENCE": { ... },
    "ADMET_FAILURE": { ... },
    "SHORT_HALFLIFE": { ... },
    "SPECIFIC_OFFTARGET": { ... },
    "PHYSICOCHEMICAL_OOB": { ... },
    "PROTEASE_VULNERABLE": { ... },
    "LOW_PREDICTED_AFFINITY": { ... },
    "LOW_DRUGLIKENESS": { ... },
}
```

### 5.2 classify_failures 확장
- interface_plddt 체크 추가
- disulfide_intact 체크 추가
- ADMET/pharmacology 메트릭 참조
- Boltz confidence 참조

### 5.3 Gate 5 (ADMET/약리학) 추가
```yaml
# gate_thresholds.yaml
gates_enabled:
  admet: true
admet_mw_max: 2000      # Da
admet_pI_range: [3.0, 10.0]
admet_protease_sites_max: 3
admet_halflife_min: 30   # minutes
```

---

## Step 6: 긍정 피드백 루프 구현 (2시간)

### 6.1 SuccessPattern 데이터클래스
```python
@dataclass
class SuccessPattern:
    top_k: int                           # 상위 K개 후보
    position_aa_frequency: dict          # {position: {AA: count}}
    common_motifs: list[str]             # 공통 모티프
    best_ddg_sequence: str               # 최고 ddG 서열
    best_ddg_value: float
    improvement_from_prev: float         # 이전 대비 개선
```

### 6.2 Reporter에 성공 패턴 추출 추가
```python
# reporter.py 래퍼
def extract_success_patterns(rank_table, top_k=5) -> SuccessPattern:
    top = rank_table.get_top_k(top_k)
    # 위치별 AA 빈도 분석
    # 공통 모티프 추출
    return SuccessPattern(...)
```

### 6.3 Planner에 전달 경로 추가
```python
# orchestrator.py
self._last_success_pattern = reporter.extract_success_patterns(rank_table)

# _adapt_agent_context("planner")에 추가:
"success_patterns": {
    "position_aa_frequency": pattern.position_aa_frequency,
    "best_sequence": pattern.best_ddg_sequence,
    "improvement": pattern.improvement_from_prev,
}
```

### 6.4 Planner가 ProteinMPNN bias로 활용
```python
# planner update_plan()
if success_patterns:
    params["mpnn_bias_per_residue"] = success_patterns.position_aa_frequency
```

---

## Step 7: 최종 보고서 작성 (30분)

### 7.1 내용
- 실험 설정 (iteration, 모델, gate threshold)
- Silo A/B 결과 비교
- 최종 후보군 (Top 10)
- Selectivity Tier 분류
- Critic 피드백 이력
- GPU/시간 리소스 사용량

### 7.2 파일
```
runs_local/
├── silob_experiment_01/       # Silo B 3 iter 결과
├── siloa_experiment_01/       # Silo A 3 iter 결과
├── selectivity_results/       # Off-target 분석
├── final_candidates.csv       # 최종 후보 10개
└── experiment_report.md       # 종합 보고서
```

---

## 병렬화 전략

```
시간 →  0h         1h         2h         3h         4h         5h
        |----------|----------|----------|----------|----------|
Step 0: [사전검증]
Step 1: [====== Silo B 3 iter ======]
Step 5:            [== Critic 확장 ==]     (병렬)
Step 6:            [== 긍정 피드백 ==]     (병렬)
Step 2:                       [========== Silo A 3 iter ==========]
Step 3:                                                    [분석]
Step 4:                                                    [Selectivity]
Step 7:                                                         [보고서]
```

**총 예상: ~6~7시간** (Silo A/B 순차 실행, Critic/피드백 병렬)

---

## 리스크 및 대응

| 리스크 | 확률 | 대응 |
|--------|------|------|
| Boltz 도킹 결과 품질 낮음 (single-seq) | 중 | ipTM < 0.5면 PyRosetta FlexPepDock 폴백 |
| DiffPepBuilder 실패 | 높음 | Boltz 단독으로 진행 |
| LLM 타임아웃 (qwen3:8b) | 중 | rule-based fallback 자동 적용 |
| QC 통과 0개 (pLDDT < 60) | 낮음 | threshold 55로 하향 |
| Rosetta ddG 전부 > -5.0 | 중 | threshold -3.0으로 완화 |
| GPU OOM | 낮음 | 96GB, ESMFold ~14GB + Boltz ~2GB = 여유 |

---

## 형상관리 체크포인트

각 Step 완료 시:
1. `git add -A && git commit -m "experiment: Step N 완료 — [결과 요약]"`
2. `git push origin main`
3. CI 통과 확인

최종:
1. `experiment_report.md` 커밋
2. `final_candidates.csv` 커밋
3. memory 업데이트
