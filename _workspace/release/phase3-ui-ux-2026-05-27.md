# Phase 3 UI/UX 연동성 + 사용자 시나리오 점검

- 점검일: 2026-05-27
- 대상: `AgenticAI4SCIENCE_pyrosetta_track/repos/ai4sci-kaeri`
- FE: `frontend`
- BE: `backend`
- 범위: FE 빌드/테스트, BE 부트, route/API 정합성, 사용자 시나리오, UX/accessibility/responsive 점검
- 원칙: 코드 수정 없음, 보고서 파일만 생성

## 1. FE 빌드 결과

### npm install

- 결과: 통과
- 출력 요약: `up to date in 573ms`
- 글로벌 패키지 설치 없음

### ESLint

- 결과: 통과
- 에러: 0
- 경고: 9
- 주요 경고:
  - `src/pages/BenchmarkPage.tsx`: `rows`, `flows`, `matrix` logical expression이 `useMemo` dependency를 매 렌더마다 변경할 수 있음
  - `src/pages/ManualSelectivityPage.tsx`: component 외 export로 Fast Refresh 경고
  - `src/pages/RunConsolePage.tsx`: `candidates` dependency 안정성 경고, effect 내부 동기 `setSelectedCand(...)` 경고
- 판단:
  - lint는 blocking 아님
  - Run Console 경고는 smoke test 실패와 같은 화면이라 Phase 5 우선 확인 필요

### build

- 결과: 통과
- TypeScript 에러: 0
- Vite build: 약 11.01초
- 경고:
  - 일부 chunk가 500 kB 초과
  - `spec-*.js`: 2,735.91 kB, gzip 774.05 kB
  - `Molstar-*.js`: 487.04 kB, gzip 133.26 kB
  - `useExperiment-*.js`: 388.58 kB, gzip 115.78 kB
- 영향:
  - 로컬 시연은 가능권
  - 원격/저사양 환경에서는 Candidate/Molstar 진입 시 첫 로드가 무거울 수 있음

### test

- 결과: 실패
- Test Files: 1 failed, 13 passed, 총 14
- Tests: 1 failed, 118 passed, 총 119
- 실패:
  - `src/App.smoke.test.tsx:95`
  - `screen.getByRole('button', { name: /more/i })`
  - Run Console Page에서 accessible name `more`에 해당하는 button을 찾지 못함
- 판단:
  - 기능 자체 고장인지 테스트 기대가 낡은 것인지는 추가 확인 필요
  - 접근성 명칭 회귀 가능성이 있음

## 2. BE 부트 결과

### 결과

- 결과: 실패
- 실패 단계: app import
- 핵심 에러:

```text
ModuleNotFoundError: No module named 'llm_benchmark'
```

- 원인:
  - `backend/main.py`가 `backend.routers.benchmark`를 import
  - `backend/routers/benchmark.py`가 `llm_benchmark.scoring.aggregate.load_phase_results`를 import
  - 현재 작업 리포 import path에 `llm_benchmark`가 없음
- 참고:
  - `.worktrees/*/llm_benchmark`는 발견되지만 현재 대상 리포 루트에는 import 가능한 `llm_benchmark`가 확인되지 않음

### /health, /docs 응답 해석

- `curl http://127.0.0.1:8001/health`: 200
- `curl http://127.0.0.1:8001/docs`: 200
- `curl http://127.0.0.1:8001/openapi.json`: 200
- 단, 이 200은 이번 backend의 정상 부팅이 아님
- 근거:
  - uvicorn은 import 단계에서 실패
  - openapi paths가 `/v1/chat/completions`, `/tokenize`, `/invocations`, `/metrics` 등 LLM serving endpoint였음
  - AI4Sci의 `/api/status`, `/api/runs`, `/api/binding_pocket/*`가 아니었음
- 판단:
  - 8001 포트에 기존 다른 서비스가 응답 중인 false positive 가능성이 큼
  - BE 부팅 판정은 N

### 정적 endpoint 수

- OpenAPI 실측은 app import 실패로 불가
- 정적 router decorator 기준 endpoint: 79개
- 주요 namespace:
  - `/api/status`, `/api/health`, `/api/runs`, `/api/runs/{run_id}`
  - `/api/runs/start`, `/api/runs/{run_id}/predicted_pass_rates`
  - `/api/pipelines/{silo}`
  - `/api/agents/{run_id}/log`, `/api/agents/{run_id}/stream`
  - `/api/selectivity/*`
  - `/api/binding_pocket/*`
  - `/api/flexpepdock/*`
  - `/api/benchmark/results`
  - `/api/wetlab/orders*`
  - `/api/strategies*`
  - `/api/validation/*`, `/api/validate/*`
  - `/api/admet/*`, `/api/pharmacology/batch`, `/api/cluster/classify`, `/api/rcsb-search`

## 3. 페이지 인벤토리

### App route / 공통 데이터 흐름

- `/`는 `/console`로 redirect
- primary nav: `/console`, `/selectivity-explorer`, `/manual-selectivity`, `/candidate`, `/run/new`, `/strategy-runner`, `/benchmark`, `/wetlab/orders`, `/binding-pocket`, `/settings`, `/about`
- legacy/direct: `/silo-a`, `/silo-b`, `/combined`, `/selectivity`
- dynamic: `/candidate/:id`, `/wetlab/orders/:id`
- React Query 중심: `src/hooks/dashboard.ts`
- App layout polling: `usePipelineStatus(2000)`가 `/api/status`, `/api/runs` 호출
- Zustand 업무 store는 확인되지 않았고 `src/stores/theme.ts`만 theme 용도

### Dashboard / Run Console (`/console`)

- 파일: `src/pages/RunConsolePage.tsx`
- fetch: `/api/status`, `/api/runs/{run_id}`, `/api/agents/{run_id}/log`, SSE `/api/agents/{run_id}/stream`, `/api/pipelines/{silo}`
- task:
  - run 상태, silo 전환, 후보 목록, 후보 상세, agent log를 한 화면에서 처리
  - Candidate Review 이동은 1 click 추가
- empty/error:
  - no run, no candidates, no agent log 문구 있음
  - 404/timeout 세분화는 약함
- 이슈:
  - smoke test의 `more` button accessible name 실패
  - 후보 row click 중심이면 keyboard 접근성 약점 가능

### BindingPocketPage (`/binding-pocket`)

- 파일:
  - `src/pages/BindingPocketPage.tsx`
  - `src/components/binding_pocket/BindingPocketEditor.tsx`
  - `src/hooks/useBindingPocket.ts`
- fetch: GET/PUT/DELETE `/api/binding_pocket/{receptor}`, POST `/api/binding_pocket/{receptor}/extract`
- BE 정합성: 정적 router에 존재
- task:
  - SSTR1~5 선택, 좌표/반경/잔기 편집, 저장/초기화/자동추출
  - 한 화면에서 완료 가능
- UX:
  - 404 시 fallback config로 작업 가능하게 설계
  - 하단 안내가 "BE API 완료 후 활성화"라고 되어 있어 실제 router 존재 상태와 메시지가 혼동됨

### Run Launcher (`/run/new`)

- 파일: `src/pages/RunLauncherPage.tsx`
- fetch: GET/PUT `/api/settings`, POST `/api/runs/start`, GET `/api/runs/{run_id}/predicted_pass_rates`, GET `/api/status` 또는 `/api/runs/{run_id}`
- task:
  - silo, iteration, seed, backbone, sequence, topM, LLM, strategy, gate, off-target 설정 후 실행
  - 작업 단계가 5개 이상으로 인지 부하 큼
- UX:
  - 실패 banner 있음
  - predicted pass rates 없음 안내 있음
  - 단일 silo 제약 안내가 있어 통합 pipeline 기대와 약간 충돌

### Selectivity Explorer (`/selectivity-explorer`)

- 파일: `src/pages/SelectivityExplorerPage.tsx`
- fetch: `/api/status` 또는 `/api/runs/{run_id}`, `/api/selectivity/jobs`, `/api/selectivity/results/{job_id}`, `/api/structures/{rel_path}`
- task:
  - tier filter, WT toggle, heatmap cell 선택, drawer 상세 확인
  - 한 화면에서 구조/sequence/score 검토 가능
- empty/error:
  - no run, no candidates 문구 있음
  - selectivity API 실패 원인 노출은 약함
- UX:
  - 후보 pose가 없으면 Molstar fallback 구조로 오인 가능
  - iPTM != Ki 경고 배너는 적절

### Manual Selectivity (`/manual-selectivity`)

- 파일:
  - `src/pages/ManualSelectivityPage.tsx`
  - `src/hooks/useSelectivity.ts`
  - `src/hooks/useFlexPepDockJob.ts`
- fetch: `/api/selectivity/receptors`, `/api/selectivity/upload`, `/api/selectivity/run`, `/api/selectivity/status/{job_id}`, `/api/selectivity/results/{job_id}`, `/api/selectivity/jobs`, `/api/flexpepdock/jobs*`
- task:
  - receptor 준비, 후보 입력, run, job tracking, 결과/ensemble 확인
  - 한 화면이지만 단계가 깊음
- UX:
  - failed job은 error 표시
  - receptor GET 실패는 조용히 무시되어 서버 미연결 원인 인지가 약함

### Candidate Viewer (`/candidate`, `/candidate/:id`)

- 파일: `src/pages/CandidatePage.tsx`
- fetch: `/api/runs`, `/api/status` 또는 `/api/runs/{run_id}`, `/api/cand03_variants/list`, `/api/admet/{seq}`, `/api/wetlab/orders/{order_id}/transition`, `/api/structures/{rel_path}`, `/api/candidate/{candidate_id}/report?run_id=...`
- BE 정합성:
  - `/api/candidate/{candidate_id}/report`는 정적 router에서 발견 안 됨
- task:
  - run/candidate 선택, Molstar 구조, ADMET, variant, wetlab 승인 전환
  - decision review는 한 화면에서 가능
- UX:
  - candidates/admet loading 있음
  - report download는 anchor라 실패 시 UI feedback 없음
  - PDB path가 없으면 실제 후보 구조 대신 fallback이 보일 수 있음

### Benchmark (`/benchmark`)

- 파일:
  - `src/pages/BenchmarkPage.tsx`
  - `backend/routers/benchmark.py`
- fetch:
  - `/api/benchmark/results?phase={phase}`
- BE 정합성:
  - router는 존재
  - 현재 BE 전체 부팅 실패 원인이 benchmark router import임
- task:
  - phase/metric toggle로 model x flow matrix 비교
  - 한 화면에서 완료 가능
- empty/error:
  - loading, failed, no rows 상태 있음

### Wetlab Orders (`/wetlab/orders`, `/wetlab/orders/:id`)

- 파일:
  - `src/pages/WetlabOrderPage.tsx`
  - `backend/routers/wetlab.py`
- fetch: `/api/wetlab/orders`, `/api/wetlab/orders/{order_id}`, `/api/wetlab/orders/{order_id}/transition`
- task:
  - order list, detail, next stage transition
  - 한 화면에서 가능
- UX:
  - loading/empty/transition error 있음
  - `text-rose-200` 같은 dark-friendly 색은 light mode 대비 주의

### About (`/about`)

- 파일: `src/pages/AboutPage.tsx`
- fetch: 없음
- task:
  - pipeline concept, formulas, evidence, constraints 설명
- UX:
  - 정보량이 매우 많음
  - 발표 중 즉석 탐색보다 사전 지정 섹션 시연이 적합

## 4. 연동성 문제

### P0: BE app import 실패

- `backend/routers/benchmark.py`의 `llm_benchmark` import가 전체 FastAPI app import를 막음
- 영향:
  - FE가 호출하는 모든 `/api/*` live 연동이 막힘
  - 현재 live demo의 최대 blocker

### P0: 8001 포트 false positive

- `/health`, `/docs` 200은 AI4Sci backend가 아니라 기존 다른 서비스 응답
- openapi에 `/v1/chat/completions`, `/tokenize`, `/metrics` 등이 보임
- 시연/검증 스크립트는 앱 고유 endpoint나 OpenAPI title 확인 필요

### P1: FE 호출, BE 미발견 endpoint

- `/api/archives/top-k`
  - 호출: `src/components/ArchivesTopKSlider.tsx`
  - BE router 발견 안 됨
- `/api/candidate/{candidate_id}/report`
  - 호출: `src/pages/CandidatePage.tsx`
  - BE router 발견 안 됨
  - anchor download라 실패 안내 없음

### P1: BE는 있으나 FE primary flow 활용이 약한 endpoint

- `/api/analysis/*`
- `/api/stability/*`
- `/api/v1/silo-a/*`
- `/api/selectivity/structure/{receptor_name}`
- 영향:
  - 기능 표면과 API 표면 간 설명 정리가 필요

### P1: Wetlab create payload provenance 손실

- `StrategyRunnerPage`는 `/api/wetlab/orders`로 strategy/mode/source job metadata를 보냄
- BE `create_order`는 `candidate_id`, `candidate_seq`, `flexpepdock_job_id` 중심으로 사용
- 요청은 성공 가능하지만 Strategy Runner -> Wetlab provenance가 일부 저장되지 않을 수 있음

## 5. UX 마찰점

- 가장 큰 마찰:
  - BE가 부팅되지 않아 FE가 mock/empty/error 중심으로 보임
- Run Console:
  - 상태 파악과 후보 상세는 1-2 click
  - test 실패와 accessibility name 이슈가 있음
- Selectivity Explorer:
  - heatmap 검토는 1-2 click
  - pose fallback 오인 위험
- Candidate Review:
  - decision review는 한 화면
  - report download 404 가능성과 실패 feedback 부재
- Run Launcher:
  - 설정 항목이 많아 5-8단계
  - 숙련자용 화면으로는 적합, 발표 중 즉석 조작은 부담
- Manual Selectivity:
  - receptor/upload/run/job/result까지 5단계 이상
  - 데모에서는 사전 준비된 job이 필요
- Benchmark:
  - UX는 간단하나 현재 BE blocker와 직접 연결
- Wetlab:
  - list/detail/transition은 1-2단계로 좋음

## 6. 접근성·반응형 점검 결과

### Accessibility

- 긍정:
  - App live badge: `role="status"`, `aria-live="polite"`
  - Run selector: `aria-expanded`, `aria-haspopup`, `role="listbox"`
  - Candidate selector: `role="group"`, `aria-pressed`
  - CandidateTable: `aria-sort`, mobile list, keyboard handler 일부 구현
  - Binding Pocket: tab/form/status 접근성 설계가 명확
  - modal 계열: `role="dialog"`, `aria-modal`
- 문제:
  - Run Console smoke test가 `/more/i` button을 찾지 못함
  - Benchmark hover detail은 keyboard focus equivalent가 약함
  - 후보 row click 중심 UI는 keyboard 접근성 추가 확인 필요
  - 일부 icon-only button의 `aria-label` 일관성 점검 필요

### 반응형

- 긍정:
  - `sm:`, `md:`, `lg:`, `xl:` breakpoint 사용 다수
  - `flex-wrap`, `overflow-x-auto`, `min-w-[920px]` 활용
  - CandidateTable은 desktop table/mobile card 분리
- 문제:
  - primary nav item 수가 많고 nav 자체 horizontal overflow 처리가 명확하지 않음
  - Benchmark matrix는 모바일에서 가로 스크롤 전제
  - About은 모바일에서 스크롤 깊이가 큼

### 라이트 모드 / 색상 대비

- 긍정:
  - light/dark token 분리
  - light 기본 본문 대비는 대체로 양호
  - `--text #1c1917`, `--text-mute #57534e`, `--bg #fafaf9`
- 주의:
  - `text-amber-200`, `text-rose-200` 같은 dark-theme 친화 색이 일부 존재
  - 10px metadata와 `text-text-dim` 조합은 발표 화면에서 읽기 어려울 수 있음
  - disabled opacity 60%, heatmap 내부 text 대비는 추가 측정 권장

### 로딩/빈 상태

- spinner/text loading:
  - App lazy loader, Candidate, Wetlab, Benchmark 등
- skeleton:
  - 명시적 skeleton은 거의 없음
- empty:
  - Run Console, Selectivity Explorer, Candidate, Benchmark, Wetlab에 기본 empty 문구 있음
- 문제:
  - timeout/404/no data가 같은 계열 메시지로 합쳐지는 화면이 있음
  - report download 실패처럼 anchor 기반 action은 화면 내 오류 표시가 없음

## 7. 한 줄 결론

- 현재 FE/BE가 박사 청자에게 시연 가능한 상태인가: Partial
- 이유:
  - FE build는 통과
  - FE test는 1건 실패
  - BE는 현재 환경에서 import 실패
  - UI shell과 페이지 구성은 충분하지만 live API end-to-end는 막혀 있음

## 8. 우선순위 fix 5건

1. P0: `llm_benchmark` import 문제 해결
   - benchmark router dependency를 정식 포함하거나 optional/fallback 처리
   - app import 실패가 전체 API를 막지 않게 해야 함

2. P0: 포트/헬스체크 false positive 방지
   - `/health` 200만 보지 말고 OpenAPI title 또는 `/api/health` 등 앱 고유 endpoint 확인

3. P1: Run Console smoke/accessibility 실패 처리
   - `more` button accessible name 기대와 실제 UI를 맞춤
   - keyboard 접근성도 함께 확인

4. P1: 누락 endpoint 정리
   - `/api/archives/top-k`
   - `/api/candidate/{candidate_id}/report`
   - 구현하거나 UI에서 비활성/미구현 안내로 전환

5. P2: Molstar fallback 오인 방지
   - 후보 PDB가 없을 때 `7XNA`를 실제 후보 구조처럼 보이지 않게 구분
   - reference/no-pose 상태를 명시

## 마지막 stdout 5줄

- FE 빌드 통과 여부: Y, 에러 0, lint 경고 9, test 실패 1
- BE 부팅 여부: N, 정적 endpoint 79개, 실측 OpenAPI 불가
- FE-BE 연동 완전성: Partial
- 가장 큰 UX 마찰점 1개: BE import 실패로 live demo가 mock/empty/error 상태에 머무는 점
- 시연 가능 여부: Partial
