# BE P0 Fix Report - 2026-05-27
## 1. Scope
- Target: `AgenticAI4SCIENCE_pyrosetta_track/repos/ai4sci-kaeri/backend`
- Meeting target: 2026-05-28 D-1 demo recovery.
- Branch observed: `docs/schrodinger-proposal-d2-20260526`.
- Conda env: `bio-tools`.
- Applied only P0-A and P0-B fixes.
- No git commit/push, DB migration, env var addition, or unrelated endpoint creation.
- P1/P2 not touched: Run Console accessibility, missing endpoints, Molstar fallback.
## 2. Phase 3 Input
- Read full report: `_workspace/release/phase3-ui-ux-2026-05-27.md`.
- P0-A: BE app import blocked by `backend.routers.benchmark`.
- Root error: `ModuleNotFoundError: No module named 'llm_benchmark'`.
- P0-B: plain `/health`/`/docs`/`/openapi.json` 200 can be a false positive from another service.
- Required correction: app-specific health identity plus AI4Sci OpenAPI/path verification.
## 3. P0-A Fact Check
- Before fix, app import failed with:
```text
ModuleNotFoundError: No module named 'llm_benchmark'
```
- `llm_benchmark` directories exist in worktrees and at outer repo root:
```text
./.worktrees/.../llm_benchmark
./_workspace/ai4sci-kaeri-fe-ef/llm_benchmark
./llm_benchmark
```
- BE demo working directory is nested:
```text
AgenticAI4SCIENCE_pyrosetta_track/repos/ai4sci-kaeri
```
- Therefore outer `./llm_benchmark` is not importable from the demo BE working directory.
- Outer module size check:
```text
4.4G llm_benchmark
```
- Copying the module was rejected as too broad for a D-1 boot fix.
- Git history check:
```text
76d1923 feat: LLM benchmark 실험 인프라 + 184개 실험 완료 + V2 파이프라인
7c30433 fix(migration): App.tsx conflict resolve + benchmark.py lazy llm_benchmark import
```
- Chosen fix: router optional handling, not module restore.
## 4. P0-A Code Change
- File: `backend/routers/benchmark.py`
- Lines after change: 8-15, 72-77.
- Before:
```python
from fastapi import APIRouter, Query
from llm_benchmark.scoring.aggregate import load_phase_results
```
- After:
```python
from fastapi import APIRouter, HTTPException, Query
try:
    from llm_benchmark.scoring.aggregate import load_phase_results
    _BENCHMARK_IMPORT_ERROR: Exception | None = None
except Exception as exc:  # pragma: no cover - depends on optional local benchmark tree
    load_phase_results = None  # type: ignore[assignment]
    _BENCHMARK_IMPORT_ERROR = exc
```
- Before:
```python
def _load_results_for_alias(phase_alias: str) -> list[dict[str, Any]]:
    results: list[dict[str, Any]] = []
```
- After:
```python
def _load_results_for_alias(phase_alias: str) -> list[dict[str, Any]]:
    if load_phase_results is None:
        raise HTTPException(
            status_code=503,
            detail=f"llm_benchmark is unavailable: {_BENCHMARK_IMPORT_ERROR}",
        )
    results: list[dict[str, Any]] = []
```
- Effect: app import and uvicorn boot succeed without `llm_benchmark`.
- `/api/benchmark/results` remains registered but returns 503 until the optional benchmark tree is restored.
## 5. P0-B Code Change
- File: `backend/routers/status.py`
- Lines after change: 16-17, 167-174.
- Before:
```python
@router.get("/health")
def health():
    return {"status": "ok", "timestamp": time.time()}
```
- After:
```python
SERVICE_NAME = "ai4sci-kaeri-backend"
SERVICE_VERSION = "2.0.0"
@router.get("/health")
def health():
    return {
        "status": "ok",
        "service": SERVICE_NAME,
        "version": SERVICE_VERSION,
        "timestamp": time.time(),
    }
```
- Effect: `/api/health` now exposes app identity.
- Demo validation should require `service == "ai4sci-kaeri-backend"`.
## 6. Verification - Import
- Command:
```bash
cd /home/dongjukim/Documents/workspace/repos/SST14-M_scr/AgenticAI4SCIENCE_pyrosetta_track/repos/ai4sci-kaeri
source /home/dongjukim/miniforge3/etc/profile.d/conda.sh && conda activate bio-tools
python -c "from backend.main import app; print('OK', len(app.routes))"
```
- Stdout:
```text
OK 83
```
## 7. Verification - Port 8001
- `ss` and `lsof` were unavailable in this shell, so a Python socket check was used.
- Stdout:
```text
8001 free: ConnectionRefusedError
```
## 8. Verification - Uvicorn, Health, OpenAPI
- Uvicorn command:
```bash
timeout 15 python -m uvicorn backend.main:app --host 127.0.0.1 --port 8001 --log-level info
```
- `/api/health` stdout:
```json
{
    "status": "ok",
    "service": "ai4sci-kaeri-backend",
    "version": "2.0.0",
    "timestamp": 1779872424.373073
}
```
- `/openapi.json` stdout:
```text
title: AI4Sci Pipeline Monitor
paths: 72
has /api/runs: True
has /api/binding_pocket/{receptor}: True
```
- Uvicorn excerpt:
```text
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8001 (Press CTRL+C to quit)
INFO:     127.0.0.1:34744 - "GET /api/health HTTP/1.1" 200 OK
INFO:     127.0.0.1:34750 - "GET /openapi.json HTTP/1.1" 200 OK
INFO:     Application shutdown complete.
```
## 9. Verification - Benchmark Isolation
- TestClient stdout:
```text
503
{'error': 'HTTPException', 'detail': "llm_benchmark is unavailable: No module named 'llm_benchmark'", 'status_code': 503}
```
- This is expected for optional router handling.
- It confirms missing benchmark dependency no longer blocks the backend.
## 10. Verification - Regression
- Command:
```bash
cd /home/dongjukim/Documents/workspace/repos/SST14-M_scr
source /home/dongjukim/miniforge3/etc/profile.d/conda.sh && conda activate bio-tools
python3 -m pytest pipeline_local/tests/ --tb=no -q
```
- Stdout tail:
```text
736 passed, 5 skipped, 2 xfailed, 15 warnings in 59.80s
```
## 11. Demo Boot Command
```bash
cd /home/dongjukim/Documents/workspace/repos/SST14-M_scr/AgenticAI4SCIENCE_pyrosetta_track/repos/ai4sci-kaeri && source /home/dongjukim/miniforge3/etc/profile.d/conda.sh && conda activate bio-tools && python -m uvicorn backend.main:app --host 127.0.0.1 --port 8001 --log-level info
```
## 12. Demo Validation Commands
```bash
curl -s http://127.0.0.1:8001/api/health | python3 -m json.tool
curl -s http://127.0.0.1:8001/openapi.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['info']['title']); print('/api/runs' in d['paths'])"
```
- Required health identity: `service == "ai4sci-kaeri-backend"`.
- Required OpenAPI identity: title `AI4Sci Pipeline Monitor`.
- Required app path check: `/api/runs` present.
## 13. Residual Risk
- `/api/benchmark/results` returns 503 unless `llm_benchmark` is restored or made importable.
- This is acceptable only if the 2026-05-28 demo does not require live Benchmark page data.
- If live Benchmark page data is required, restore the benchmark module in a separate scoped change.
- No P1/P2 issues were fixed in this patch.
- Existing unrelated dirty worktree files were left untouched.
## 14. Final 5 Lines
- P0-A 해결 방식: router optional 처리; app boot 유지, `/api/benchmark/results`만 503.
- P0-B 해결 방식: `/api/health`에 `service: ai4sci-kaeri-backend`, `version: 2.0.0` 추가.
- BE 부팅 + `/api/health` 응답 성공 여부: 성공; OpenAPI title `AI4Sci Pipeline Monitor`, `/api/runs` 확인.
- `pipeline_local` pytest 736 PASS 유지 여부: 성공; `736 passed, 5 skipped, 2 xfailed`.
- 5/28 시연용 BE 부팅 한 줄 명령: `cd /home/dongjukim/Documents/workspace/repos/SST14-M_scr/AgenticAI4SCIENCE_pyrosetta_track/repos/ai4sci-kaeri && source /home/dongjukim/miniforge3/etc/profile.d/conda.sh && conda activate bio-tools && python -m uvicorn backend.main:app --host 127.0.0.1 --port 8001 --log-level info`
